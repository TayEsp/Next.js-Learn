/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  //aqui podemos definir todas as variaveis de ambiente
  //como o user e password de um banco de dados, como seu caminho
  //ou podemos desabilitar algo
};

export default nextConfig;
